{% extends "layout.njk" %}

{% block head %}

    <title>Foodfy - Admin {{ chef.name }}</title>
    <link rel="stylesheet" href="/css/foodesign.css">

{% endblock %}


{% block content %}
    
  <div class="header">
    <h2>Chef: {{chef.name}}</h2>
    <a href="/admin/chefs/{{chef.id}}/edit">Editar Chef</a>
  </div>

  <div class="chef-body">
    <img src="{{chef.avatar_url}}" alt="Foto do chef">
    <h1>{{chef.name}}</h1>
    <p>{{total}} receitas</p>
  </div>

  <h1 class="receipts">Receitas</h1>

  <section class="index" id="chef-portfolio">
      {% for dessert in receipts %}
        <div class="index-item">
            <img src="{{ dessert.images }}" alt="">
            <div>
                <h2>{{ dessert.name }}</h2>
                <a href="/admin/recipes/{{ dessert.idd }}">Visualizar</a>
            </div>
        </div>
      {% endfor %}
  </section>


  <script src="/script.js"></script>

{% endblock %}